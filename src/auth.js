/**Como funciona uma autenticação no Frontend? Geralmente o usuário se autentica e aplicação leva dados de e-mail para um servidor e esse servidor retorna um Token JWT
 * este token é guardado no storage da aplicação e geralmente na Web é salvo no local storage e qualquer requisição subsequente ele envia esse token junto para o nosso backend
 */

    /**Aqui haveria toda a lógica */
export const isAuthenticated = () => true;
